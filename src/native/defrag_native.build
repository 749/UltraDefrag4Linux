-- UltraDefrag boot time interface build options
name = "defrag_native"; target_type = "native"

-- list of directories containing headers the program relies on
includes = { "include", "native", "dll\\udefrag", "dll\\zenwinx" }

libs = { "ntdll", "zenwinx", "udefrag" }
